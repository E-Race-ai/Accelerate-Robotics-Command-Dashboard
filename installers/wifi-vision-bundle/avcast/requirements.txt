zeroconf>=0.130
soco>=0.30
PyChromecast>=13
fastapi>=0.110
uvicorn[standard]>=0.30
requests>=2.31
aiohttp>=3.9
# WHY: macOS ships Python 3.9; backend.py uses PEP 604 `str | None` annotations
# (3.10+). eval_type_backport teaches FastAPI/Pydantic to evaluate that on 3.9.
eval_type_backport>=0.2
# WHY: FastAPI requires python-multipart to register endpoints that take
# UploadFile/File/Form params (the printer print-job upload uses these).
python-multipart>=0.0.9
# WHY: lazy-imported inside printers.py — needed for the printer panel
# (CMYK toner gauges, paper trays, saddle-staple status via SNMP).
pysnmp>=6.0
# WHY: lazy-imported inside printers.py — needed for the saddle-stitch
# booklet wizard (2-up imposition runs through pypdf).
pypdf>=4.0
# WHY: server-side PDF generation for the WiFi Audit "Download report"
# button — pure Python, no native deps, identical output across browsers.
reportlab>=4.0
# WHY: real control of Apple TVs and HomePods (volume + transport + now
# playing). Requires a one-time pairing handshake per device — pyatv.scan
# discovers devices, pyatv.pair walks through PIN entry, credentials are
# persisted to ~/Library/Application Support/WiFiVisionExtreme/atv-creds.json
# so subsequent connections are silent.
pyatv>=0.15
